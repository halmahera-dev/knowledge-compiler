# Dependencies
node_modules/

# Build outputs
dist/
.output/
.next/
.nitro/
.tanstack/
.mastra/
.turbo/
.vinxi/
mastra.db
mastra.db-*

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Environment variables
.env
.env.*
!.env.example

# Local model cache for the embedding fallback
.cache/
apps/api/.model-cache/

# IDEs and editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Package manager lockfiles
package-lock.json
yarn.lock

# Misc
*.pem

# Holds a real CockroachDB Cloud service-account key. Same rule as .env: the
# working copy is yours, the committed one is .mcp.json.example.
.mcp.json

# Built from apps/extension by scripts/pack-extension.mjs on every client build.
# Committing it would let the download drift behind the source.
apps/client/public/knowledge-compiler-extension.zip
